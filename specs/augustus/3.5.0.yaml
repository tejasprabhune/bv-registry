name: augustus
version: 3.5.0
channels:
  - https://conda.anaconda.org/bioconda
  - https://conda.anaconda.org/conda-forge
packages:
  - augustus ==3.5.0
entrypoint:
  command: /opt/conda/bin/augustus
platform: linux/amd64
